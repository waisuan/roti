language: java
before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar
script:
  - ./gradlew test build

services:
  - postgresql

before_script:
  - psql -c 'create database roti_test;' -U postgres

env:
  global:
    - DB_PWD=""